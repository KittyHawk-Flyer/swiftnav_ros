# Building and Installing the SBP Library (libsbp)

In some other folder...
```
git clone git@github.com:PaulBouchier/libsbp.git
cd libsbp/
cd c/
mkdir build
cd build/
cmake ..
make
sudo make install
```
